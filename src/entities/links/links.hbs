{{#each links as |link|}}
  <div>
    <h2>{{link.title}}</h2>
    {{#each link.items as |item|}}
      <div>
        <a href='{{item.link}}' target='_blank'>{{item.title}}</a>
        <span>{{item.description}}</span>
      </div>
    {{/each}}
  </div>
{{/each}}